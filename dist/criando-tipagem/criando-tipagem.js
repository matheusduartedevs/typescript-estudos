// tipo literal
let literal;
// literal = 'Hi, World' => erro
// union types
let option;
option = 2;
option = false;
let planet;
if (planet === 'Terra') {
    console.log('Planeta Terra');
}
function greet(callback) {
    let name = 'matheus';
    callback(name);
}
